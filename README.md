# Client-Orchestrated MCP Assistant

Public portfolio case study of a local Model Context Protocol (MCP) server that equips AI desktop clients with controlled tools to search, inspect, download and notify users about PDF documents.

> **Repository status:** this public version documents the architecture and publishes selected reusable components reconstructed from a private working implementation. It intentionally excludes organization-specific sources, downloaded documents